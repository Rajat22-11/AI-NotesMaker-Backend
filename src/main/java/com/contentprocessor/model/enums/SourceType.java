package com.contentprocessor.model.enums;

public enum SourceType {
    YOUTUBE,
    PDF,
    TEXT,
    VIDEO,
    AUDIO,
    // Aliases used by tests / older code
    VIDEO_FILE,
    AUDIO_FILE,
    PDF_FILE
}